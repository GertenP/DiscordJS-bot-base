{
  "name": "discord-ticket-bot",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " rm -r dist/ && tsc && copyfiles -u 1 \"src/**/*\" \"src/*\" dist/ && find dist -name \"*.ts\" -type f -delete",
    "start": "rm -r dist/ && tsc && copyfiles -u 1 \"src/**/*\" \"src/*\" dist/ && find dist -name \"*.ts\" -type f -delete && node dist/deploy-commands.js && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "discord.js": "^14.19.3",
    "dotenv": "^16.5.0"
  }
}
